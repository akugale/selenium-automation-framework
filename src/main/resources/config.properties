# Active environment overlay: config-<env>.properties (env from -Denv= or TEST_ENV, default dev)
# --- Application URLs (Angular IR) ---
login.url=https://172.26.100.115:8046/CareRating/auth/login
app.base.url=https://172.26.100.115:8046/CareRating/

# --- WebDriver ---
browser=chrome
headless=false
implicit.wait=0
explicit.wait=15

# --- Feature flags ---
workflow.e2e.enabled=false
download.dir=downloads

# --- API layer (RestAssured) — set api.enabled=true when backend base URI is ready ---
api.enabled=false
api.base.uri=https://172.26.100.115:8046/
api.bearer.token=

# --- Database (SQL Server) — set db.enabled=true for JDBC checks ---
db.enabled=false
db.url=jdbc:sqlserver://localhost:1433;databaseName=CareIR;encrypt=true;trustServerCertificate=true
db.user=sa
db.password=
